package component

type LinkAttrs struct {
	Href string
}

templ Link(la LinkAttrs) {
	<a href={ templ.SafeURL(la.Href) } class="font-semibold text-teal-500 hover:text-teal-400">
		{ children... }
	</a>
}
